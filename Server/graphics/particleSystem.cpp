/*
const unsigned int maxParticles = ;

struct particle {
  vector4f position;
  vector4f color;
  vector3f velocity;
};

std::vector<particle> particles;

void init() {
  particles.reserve(maxParticles);
  for(unsigned int = 0; i < maxParticles; i++) {
    particle p;
    particles.push_back(std::move(p));
  }
}

void drawHead() {

}

void drawInstance() {
  std::vector<GLfloat> positions;
  for(auto itr = particles.begin(); itr != particles.end(); itr++) {
    positions.insert(positions.end(), itr->begin(), itr->end()):
    positionsAttr.write(positions);
  }

}
*/